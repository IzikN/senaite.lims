services:
  - type: web
    name: senaite-lims
    env: docker # important: not "python"
    plan: free
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    healthCheckPath: /
    healthCheckTimeoutSeconds: 120
    ports:
      - 8080
